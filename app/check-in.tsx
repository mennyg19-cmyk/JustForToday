import { CheckInFlow } from '@/features/checkin/CheckInFlow';

/** Route: /check-in — daily check-in flow (commitment + reflection). */
export default function CheckInRoute() {
  return <CheckInFlow />;
}
