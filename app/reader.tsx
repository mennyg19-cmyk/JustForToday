import { ReaderScreen } from '@/features/reader/ReaderScreen';

/** Route: /reader — in-app reader for grounding readings (PDFs and web pages). */
export default function ReaderRoute() {
  return <ReaderScreen />;
}
