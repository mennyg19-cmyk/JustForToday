/**
 * Centralized card class constants.
 * Import these instead of duplicating card styling across screens.
 */
export const CARD = 'rounded-2xl p-4 bg-card border border-border';
export const CARD_MB = `${CARD} mb-4`;
export const CARD_SHADOW = `${CARD} shadow-card`;
